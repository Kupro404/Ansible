#Docker yml vars
$DB_ROOT_PASSWD: password
$DB_PASSWD: wordpress #also used for wp user password
$DB_USER: wordpress #also used for wp user
$DB_NAME: wordpress

#replace with your host machine ip
$SSH_SOURCE_IP: your_host_ip

#inventory vars
$TARGET_IP: your_target_ip
$TARGET_PASSWD: changeme 
